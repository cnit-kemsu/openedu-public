import { makeStyles } from "@material-ui/core/styles";

export const DefaultLayout = makeStyles({
  root: {
    width: '100%'
  }
});

export const AdminLayout = makeStyles({
  view: {
    width: '100%'
  }
});